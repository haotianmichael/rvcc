#include <iostream>
#include <algorithm>

int 
main() {

    int a, b, c, d, f, N, res;
    int array[100];
    a = 2 * (9 + array[3+4*8]/  3 - 5)  + 4;
    c = 2 * (9 + a/  3 - 5)  + 4;
    b = 4 / c;
    d = 5;
    f = (a - 34) * 20/ 3;
    N = 9;

    if(a + b > N + array[4]) {
        array[a + b + c + d + f]  = a;
        res = a + b;
    }else {
        array[(5 - a) / 4 * 3] = array[a+(f -array[a + b])* f/ d];
        res = a -  b;
    }

}
