#include <iostream>
#include <algorithm>

int 
main() {

    int tmpA, tmpB, a, b, c, d, f, key, i;
    int array[100], q[100];
    a = 2 * (9 + array[3+4*8]/  3 - 5)  + 4;
    c = 2 * (9 + a/  3 - 5)  + 4;
    b = 4 / c;
    d = 5;
    f = (a - 34) * 20/ 3;
    array[a + b + c + d + f]  = a;
    array[(5 - a) / 4 * 3] = array[a+(f -array[a + b])* f/ d];

}
