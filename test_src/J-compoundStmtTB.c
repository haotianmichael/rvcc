#include<stdio.h>

int 
main() {

    int a, b, c, d, f, N, res;
    int array[100];
    a = 2 * (9 + array[3+4*8]/3)  + 4;
    b = 4 / a;
    c = 2 * (9 + a/  3 - 2)  + 4;
    d = 5;
    f = (a - 34) * 20/ 3;
    N = 9;

    if(a + b > N + array[4]) {
        array[a + b + c + d + f]  = a;
        res = a + b;
    }else {
        array[(5 - a) / 4 * 3] = array[a+(f -array[a + b])* f/ d];
        res = a -  b;
    }


    while(array[(a - 34)*20] != array[N+res]) {
        array[(5 - a) / 4 * 3] = array[a+(f -array[a + b])* f/ d];
        array[(5 - a) / 4 * 3] = array[a+(f -array[a + b])* f/ d];
        res = a -  b;
    }
    printf("haotian is a good man");

    return ((a - 34) * 20/ 3);
}
